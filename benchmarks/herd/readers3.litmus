C readers/3

{ [x] = 0; }

P0 (atomic_int *x)
{
	atomic_store_explicit(x, 42, memory_order_release);
}

P1 (atomic_int *x)
{
	int r1 = atomic_load_explicit(x, memory_order_acquire);
}

P2 (atomic_int *x)
{
	int r1 = atomic_load_explicit(x, memory_order_acquire);
}

P3 (atomic_int *x)
{
	int r1 = atomic_load_explicit(x, memory_order_acquire);
}