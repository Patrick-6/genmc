C cii

{ [x] = 0; [one] = 1; }

P0 (atomic_int *x, volatile int *one)
{
	int r1 = atomic_compare_exchange_strong_explicit(x, one, 2, memory_order_acquire,
	       	 					    	    memory_order_acquire);
}

P1 (atomic_int *x)
{
	int r1 = atomic_fetch_add_explicit(x, 1, memory_order_relaxed);
}

P2 (atomic_int *x)
{
	int r1 = atomic_fetch_add_explicit(x, 1, memory_order_relaxed);
}